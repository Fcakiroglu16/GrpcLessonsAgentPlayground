# gRPCLessons

## Developer Setup

After cloning the repo, run the following command once:

```bash
git config core.hooksPath hooks
```

This activates the pre-commit hook that automatically formats `.cs` files before each commit.
